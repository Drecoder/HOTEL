// Kafka topic and group constants
export const KAFKA_TOPICS = {
  ROOM_EVENTS: 'room-events',
  BOOKING_EVENTS: 'booking-events',
   ROOM_READY_EVENT: 'room_ready', 
};

export const KAFKA_GROUPS = {
  ROOM_CONSUMER: 'hotel-ops-consumer-group-server',
};
